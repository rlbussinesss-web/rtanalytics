# Copy this file to infra/.env before running `docker compose up`.

# TimescaleDB / Postgres
POSTGRES_USER=rtanalytics
POSTGRES_PASSWORD=rtanalytics_dev_password
POSTGRES_DB=rtanalytics
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379
