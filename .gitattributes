# Files consumed by Linux containers must keep LF endings regardless of the
# checkout platform — CRLF in a Dockerfile or shell script breaks the build.
* text=auto eol=lf
Dockerfile text eol=lf
*.sh text eol=lf
*.conf text eol=lf
*.template text eol=lf
